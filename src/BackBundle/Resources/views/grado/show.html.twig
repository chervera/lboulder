{% extends 'base.html.twig' %}

{% block body %}
    <h1>Grado</h1>

    <table>
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ grado.name }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ grado.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('grado_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('grado_edit', { 'id': grado.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
